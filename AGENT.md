部署更新：已添加 `deploy.sh`。在当前目录执行 `./deploy.sh` 即可将 `plugin/` 目录同步到 WSL 路径 `/mnt/e/ChromeExtension/EnhancedCaiXin`（对应 Windows 路径 `E:\ChromeExtension\EnhancedCaiXin`）。如需临时指定其他部署目录，可使用 `DEPLOY_DIR=/path/to/target ./deploy.sh`。

# AGENTS.md

## 1. 项目概述

本仓库用于：。
我在开发一个浏览器插件，当前环境是wsl, 这里是开发环境，在部署的时候，你需要将插件文件夹复制到E:\ChromeExtension\EnhancedCaiXin这个路径。
在wsl中是`/mnt/e/ChromeExtension/EnhancedCaiXin`这个路径，这样浏览器才可以读取这个插件


主要技术栈：

* 编程语言：后端go / 前端TypeScript / 数据库:mysql + docker部署
* 框架：后端框架gin / 前端Vue3

---

## 2. 仓库结构
sql: 该文件夹用于存储所有和本项目的mysql相关的文件
plugin: 该文件夹用于存储所有和浏览器插件逻辑相关的文件和代码
README.md: 说明该项目的重要信息，如果其他模块更新了，必须同步更新这里
.gitignore: 该文件存储需要被忽略的文件
front-end: 该文件夹存储所有和该显示前端代码相关的代码

当前实际结构：

* `plugin/`: 浏览器插件源码目录，部署脚本默认同步该目录。
* `front-end/`: Vue3 前端代码目录。
* `sql/migrations/`: MySQL 迁移文件目录。
* `sql/deploy_mysql.sh`: Docker MySQL 部署与迁移执行脚本。
* `deploy.sh`: 插件部署脚本。
* `README.md`: 项目说明与常用命令。
## 3. 环境配置
当前在wsl中开发代码，在部署时，要将插件文件夹复制到windows系统下特定的文件夹中


## 5. 开发规则

通用规则：

* 保持修改小而聚焦。
* 不要修改与任务无关的文件。
* 不要在未被要求时进行大规模重构。
* 不要在未被要求时格式化整个仓库。
* 不要随意重命名公共 API、文件或目录。
* 优先保持代码可读、可维护，而不是追求炫技。
* 遵循项目现有的代码风格和命名约定。
* 新增抽象前，先检查项目中是否已有可复用工具。
* 除非任务明确允许破坏兼容性，否则应保持向后兼容。

修改代码时：

* 先理解周围实现，再进行修改。
* 优先使用最小 diff。
* 注释应解释非显然的原因，而不是重复代码含义。
* 不要引入死代码、未使用导入或未使用依赖。
* 不要静默吞掉异常。
* 除非任务要求，否则不要改变现有行为。

---

## 6. 测试规则

完成任务前，应运行最小必要的验证命令。

优先级：

1. 运行与改动文件直接相关的测试。
2. 如果改动影响公共逻辑，运行更大范围的测试。
3. 如果项目使用 lint 或类型检查，也应运行对应命令。
4. 如果无法运行测试，需要明确说明原因。

不要声称测试通过，除非确实运行过测试。

新增或修复功能时：

* 适当新增或更新测试。
* 修复 bug 时优先添加回归测试。
* 测试应保持确定性。
* 除非项目已有惯例，否则不要让测试依赖外部网络。

---


* 除非明确要求，否则不要提交 commit。
* 除非明确要求，否则不要执行 amend、squash、rebase、reset 或 force-push。
* 不要丢弃用户已有改动。
* 如果工作区存在与当前任务无关的用户改动，避免触碰这些文件。
* 如果遇到合并冲突，先说明冲突内容，再处理。

多 agent / 多任务并行时：

* 一个任务使用一个独立分支或 worktree。
* 每个任务只修改小范围文件。
* 避免多个任务同时修改共享文件，例如：

  * 依赖文件
  * lock 文件
  * README
  * 核心配置文件
  * 公共入口文件

---

## 8. 依赖管理规则

* 不要随意新增依赖。
* 优先使用标准库或项目已有依赖。
* 如果必须新增依赖，需要说明原因。
* 不要升级与任务无关的依赖。
* 不要在没有必要时重新生成 lock 文件。
* 尊重项目已有的包管理方式。

## 9. 安全规则
避免执行以下危险命令：

```bash
rm -rf /
rm -rf ~
rm -rf .git
git reset --hard
git clean -fdx
docker system prune -a
DROP DATABASE
```

如果某个破坏性操作看起来是必要的，应先停止并请求确认。

---

## 10. 网络和外部服务规则


## 11. 文档规则

当行为、配置、命令、公共 API 或使用方式发生变化时，应同步更新文档。

文档应当：

* 准确
* 简洁
* 可操作
* 与项目已有术语保持一致

除非任务明确要求重写文档，否则不要大规模改写文档文件。

---

## 12. 代码风格规则

遵循仓库已有风格。

通用偏好：

* 命名清晰，不要过度缩写。
* 控制流简单，避免过深嵌套。
* 错误处理要明确。
* 函数职责单一。
* 避免不必要的全局可变状态。
* 避免无意义的抽象。


## 15. 最终回复格式

每次任务结束时，按以下格式汇报：

```text
Summary:
- ...

Files changed:
- ...

Commands run[可选]:
- ...

Test results[可选]:
- ...

Remaining risks[可选]:
- ...
```
